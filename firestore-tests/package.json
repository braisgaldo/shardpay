{
  "name": "shardpay-rules-tests",
  "private": true,
  "type": "module",
  "description": "Pruebas de las reglas de seguridad de Firestore contra el emulador (ADR-0009).",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project shardpay-rules \"node --test rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.2",
    "firebase-tools": "^13.29.1"
  }
}
